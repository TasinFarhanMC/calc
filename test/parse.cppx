#include <calc.h>
#include <cassert>
#include <iostream>

int main(int argc, char *argv[]) {
  const char str[] = "12.232 + 123.3";
  CalcBufs bufs = calc_parse_ascii(str, sizeof(str) - 1).ok;

  // assert(bufs.nums.data[0].val == (calc_usize)(12.25f * (1UL << 32)));

  return 0;
}
